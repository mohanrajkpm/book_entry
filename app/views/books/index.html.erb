<div class="container">
  <h2>Basic Table</h2>
  <p>The .table class adds basic styling (light padding and only horizontal dividers) to a table:</p>            
  <table class="table">
    <thead>
      <tr>
        <th>Name</th>
        <th>Author</th>
        <th>ISBN</th>
        <th>Price</th>
        <th>Category</th>
        <th>Publish</th>
      </tr>
    </thead>
    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td><%= book.name %></td>
          <td><%= book.author %></td>
          <td><%= book.isbn %></td>
          <td><%= book.price.to_i %></td>
          <td><%= book.category %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>